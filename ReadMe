《角标频控功能实现》

一个页面顶部有4个顶导按钮，每个按钮支持右上角展示角标文案。如果角标文案下发长度大于0，则该按钮展示对应角标，否则不展示。那么4个按钮最多展示4个角标文案。

需要完成以下功能的实现：

​    支持按优先级控制可同时展示在按钮上的角标文案最大个数。如最大展示个数配置2，每个按钮都下发了角标文案，且前两个按钮角标优先级较高，那么此种情况下仅前两个按钮可以展示出角标。

在Demo工程中，已完成数据解析和角标数据渲染展示功能。请在TopNavView.m类中对角标数据进行逻辑处理，完成该功能的实现。角标优先级响应NavTabItemModel中的labelPriority字段(数值越大,优先级越低)

执行用例：

1.TopNavView.m类中修改kTopNavViewTagLimit的值为1， 2， 3， 4, 100验证对应效果；

2.ViewController的dataSource数据源方法中将删除"导航一"按钮的角标文案，验证步骤一的效果；
